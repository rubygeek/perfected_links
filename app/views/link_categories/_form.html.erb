<%= form_with(model: link_category, local: true) do |form| %>
  <% if link_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(link_category.errors.count, "error") %> prohibited this link_category from being saved:</h2>

      <ul>
        <% link_category.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
